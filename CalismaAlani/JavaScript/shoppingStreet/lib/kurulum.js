import { binalar,sokak } from "../script";

// array.forEach(element => {
    
// });

    
    // Define how many grid spaces you want
    const numberOfItems = 6;

    // Loop to create and add a div into each grid space
    for (let i = 0; i < numberOfItems; i++) {
        sokak.appendChild(document.createElement("div"));
}